import { Container } from "@mui/material";
import BlogListHeader from "../components/Header";

export const BlogList = ({}) => {
  return (
    <Container fixed>
      <BlogListHeader />
    </Container>
  );
};
